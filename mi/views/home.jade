style.
  h4{
    max-height: 1em;
    overflow: auto;
  }

  h4 span {
    font-size:12px;
    
    
  }

script.
  $(document).ready(function(){
    $("#criterio").focus();
  })

section
  .logoContainer
    div
      img.logoHome(src='img/logo-ch.png')    
    .row(style='margin:0px')
      .col-lg-6.col-sm-10.col-lg-offset-3.col-sm-offset-1        
        form(ng-submit="Encontrar(criterio)")
          .input-group
            input#criterio.form-control(type='text', placeholder='...' ng-model='criterio')
            span.input-group-btn
              button.btn.btn-primary(type='button' ng-click='Encontrar(criterio)') Buscar!
    
    .row(style='margin:10px')
      .col-lg-6.col-sm-10.col-lg-offset-3.col-sm-offset-1.text-center
        a(href="#/ver_cat") Navegar por Categorias